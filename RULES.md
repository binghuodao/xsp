# XSP 交易规则文档

> **2026-08-03 变更：回测口径切换（回测 = app 重放）**
> 全篇回测数字来源改为 **app 实时规则重放**（`python3 tests/sim_reports_full.py --no-net --period 7y`），
> 不再引用 hybrid.py 独立回测（其入场无 direction/互斥闸，与 app 实际执行不符）。产物:
> `tests/sim_reports_full/sim_rpt_2021~2026.txt` + `index_7y.txt` + `backtest_stats_7y.txt`。
> 窗口 = ^XSP 数据全量可用区间（yfinance 对 ^XSP 最早只到 2021-03-01，「2020-08 起 6 年」不可达）:
> **2021-06-01 → 2026-08-03，1299 交易日**。注意：VIX 百分位等指标随窗口历史长度变化，
> 换窗口会整体偏移数字，本窗口为规范窗口。
> - **三层合计: 185tr, +$10,060, WR 144/185**
>   - 趋势（$5k SPXL + 14DTE w15价差）: **9tr +$1,210, WR 7/9**（滚仓 0；2026 年价差多因 is_trend<50 未开）
>   - MR（裸买CALL 7DTE + $2k SPXL）: **2tr +$370, WR 1/2**（均 2022）
>   - 崩盘（CALL价差15点21DTE + $2k SPXL）: **174tr +$8,480, WR 136/174, max亏 -$767**
> - 此前各「变更」日志中引用 hybrid 回测的数字（如 241tr +$35,632、趋势 21tr、崩盘 193tr、MR 27tr/24tr）**全部作废**。
> - **MR 结构性低频**：MR 信号日=恐慌日（RSI<30+VIX>20），与崩盘开仓日（XSP 单日跌>0.5%）高度重叠；
>   app 有 direction 闸 + 崩盘互斥 + 2026-07-31 双向互斥 → 恐慌日优先被崩盘层承接，5.4y 仅 2 笔（非回测误差）。

> **2026-08-02 变更：报告移除树区块**
> 7DTE 圣诞树（Christmas Tree）从盘中报告中彻底移除（渲染 + `_latest_report` 树字段 + 前端报告卡片），因三个现行策略均不使用树。
> UI 圣诞树专区 / 自选（watchlist 树组合自动加入）/ 历史功能保留，仅规则见 §5。

> **2026-07-31 变更：崩盘ETF半仓分批（期权单口不变）**
> 崩盘期权维持**单口**（首阳全平），仅 **$2k SPXL 分两半**：首阳平 $1k，剩余 $1k 在二次阳线/止损/T+4 平。
> 备选方案被否：纯期权分批 WR 78→76%（+$10,628）与双腿分批（+$10,754）WR 略降，均不及 ETF-only。
> - 崩盘: 215tr +$9,025 → **193tr +$10,162**（ETF +$4,201, OPT +$5,961；WR 78% 不变，max亏 -$780 不变，均持 2.7→3.5d）
> - ETF-only 增量 +$632 来自半仓续持；OPT +$505 仅来自槽位占用更长导致的边际入场重排。
> - 三层合计: 263tr +$34,495 → **241tr +$35,632**（趋势 21tr +$16,771 / 崩盘 193tr +$10,162 / MR 27tr +$8,699）
> app.py 崩盘出场状态机增加 `_crash_etf_scaled`（首阳后保留 entry_date/entry_price/etf_entry，槽位持续占用至ETF另一半平仓）。

> **2026-07-31 变更：MR 与崩盘互斥补齐**
> MR 入场条件增加 `_crash_entry_date is None`（app.py），崩盘持仓期间不再开 MR 仓。
> 此前崩盘已要求「无 MR 仓」才入场（同日 MR 先开则崩盘被挡），但 MR 未检查崩盘仓 → 崩盘先开时 MR 可「骑仓」。现双向互斥。
> 三层回测为独立加总，未模拟互斥；同日重叠入场日仅 9/215，互斥调整后两层合计差 −$61（0.3%），三层合计不变。

> **2026-07-31 变更：趋势入场高位过滤（BB%>80 暂缓开仓）**
> 趋势**新开仓**增加 **BB%>80（dlow>80）入场过滤**：Bollinger 上轨附近（高位）不追高，等待回落后的下一个信号再入场。
> - 过滤仅约束**新开仓**；已有持仓不受影响（滚仓照常、出场规则不变）
> - 趋势（ETF + 14DTE w15价差）: 27tr→**21tr**, +$17,765→**+$16,771**（WR 70%→71%，max亏 -$1,206→**-$1,022**，亏合计 -$6,926→-$3,540）
> - 纯ETF腿: 21tr, **+$7,249**, **WR 76%**, max亏 -$452（原 27tr +$8,208, WR 67%, max亏 -$478）
> - 三层合计: 269tr +$35,489 → **263tr +$34,495**（Crash/MR 不变）
> 选择依据：高位入场多为追高（9 笔亏损中 7 笔入场即横盘），跳过 BB%>80 后 WR 67→76%、亏损合计近减半，PnL 仅 -$959（-12%）。

> **2026-07-31 变更：裸CALL→长DTE加宽价差（资本友好过渡）**
> 趋势/崩盘从短DTE裸买/窄价差改为长DTE加宽价差（权利金占用量仍可控，互斥策略最大并发≈$5.6k<$10k）：
> - 趋势裸CALL 7DTE → **14DTE w15 CALL价差**（K1=Δ0.50 ATM，K2=K1+15，DTE≤2滚仓）: 27tr, +$15,329→**+$17,765**（WR 63%→70%，max亏 -$1,437→-$1,206，权利金 $507→$531/口，滚仓 96→37）
> - 崩盘 CALL价差5点 7DTE → **CALL价差15点 21DTE**: 215tr, +$6,629→**+$9,025**（WR 79%→78%，max亏 -$565→-$780，权利金 $220→$587/口）
> - MR 维持裸买 7DTE 不变（价差回测全部跑输裸买）
> - 三层合计: 269tr, +$30,657→**+$35,489**
> 选择依据：优先胜率（Trend 14DTE w15 WR 70% > 21DTE w15 67%；Crash 21DTE w15 WR 78% > 14DTE w15 74%）。
>
> **2026-07-31 暂缓变更（裸CALL长DTE升级，等资金充足后执行）**
> 期权 DTE 研究：裸CALL 长 DTE 更优，但权利金占用资金过大。已先以长DTE价差过渡（本次变更），裸CALL 30DTE 升级仍暂缓：
> - 趋势裸CALL 14DTE价差→30DTE: +$17,765→+$21,876（权利金 $531→$1,050/张）
> - MR 裸CALL 7DTE→14DTE: +$8,699→+$8,968（WR 85%→89%）
> - Crash 价差21DTE→裸CALL 30DTE: +$9,025→+$14,689（但 WR 78%→70%，max亏 -$780→-$1,661，权利金 $587→$1,300/口）
> - 三层全改约 +$45,500（vs 现状 +$35,489），需资金约 $12.4k（> 现 $10k 本金）
> 资金充足后按序执行：趋势 30DTE → MR 14DTE → Crash 裸C 30DTE
>
> **2026-07-30 变更：滚动裸CALL加入趋势层**
> 趋势方向同时持有$5k SPXL 3x + **1张7DTE ATM滚动裸CALL**（DTE≤2滚仓）。
> 裸CALL共享ETF的XSP出场触发条件，无独立止损。
> 6年回测（ETF + 滚动ATM CALL）: 27tr, +$15,329（OPT+$7,120, ETF+$8,208）, 63% WR, 96滚.
> 三层合计: 267tr, +$30,170.
>
> **2026-07-30 变更：崩盘止损从3%降至2.5% XSP**
> 跟踪止损对所有crash回测无影响（首阳即出已是最紧）。固定止损2.5%为最优：
> 214tr, +$6,411, WR79%, max亏 -$565.
>
> **2026-07-28 变更：去除摆动交易**
> 6 年回测摆动 33 笔净亏 −$266。信号 B（DI 交叉）因 double_day 确认率=0% 从未触发。
> 信号 C（BB 边缘+RSI）净亏 −$266（SHORT 15笔 −$232，LONG 18笔 −$34）。
> 摆动增加复杂度无净收益，纯趋势 CALL 27笔 $+7,248 更高，已移除。
> 旧规则见 §9（保留作历史参考）。

## 1. 核心参数

### 分数权重
| 指标 | 权重 | 分档（100 / 75 / 50 / 25 / 0） |
|------|------|----------------------------------|
| ADX  | 30%  | ≥30 / ≥25 / ≥20 / ≥15 / <15 |
| ER   | 20%  | ≥0.70 / ≥0.55 / ≥0.35 / ≥0.20 / <0.20 |
| BBW  | 15%  | ≥45 / ≥30 / ≥18 / ≥10 / <10 |
| Dev  | 15%  | ≥3.0 / ≥1.5 / ≥0.8 / ≥0.3 / <0.3 |
| VR   | 10%  | ≥2.0 / ≥1.3 / ≥0.8 / ≥0.5 / <0.5 |

- 公式：`score = 5 + Σ(分数(sub_val, T[k]) × W[k])`，四舍五入取整

### 关键阈值
| 参数 | 值 |
|------|----|
| is_trend | score ≥ 50 |
| 显示标签 Trending | score ≥ 65 |
| 近轨 ATR 乘数 | ATR14 × 0.60 |
| 近轨 BW 回退 | BW × 0.10（ATR14 不可用时） |
| 近轨方向分数下限 | score ≥ 60 |
| VIX 百分位阈值 | > 75 |
| 期权 DTE | 14 天（趋势CALL价差）/ 21 天（崩盘CALL价差）/ 7 天（MR裸买） |

---

## 2. 方向逻辑（4 层判断，从上到下）

### Level 1 — 趋势（非近轨 + is_trend）
```
非近轨 + score ≥ 50 → 趋势方向
  DI diff > 0 + di_diff_prev > 0 + Price > SMA50 → CALL（需 2 天连续 DI+ 确认 + 价格在 50 日均线上方）
  DI diff > 0 + (di_diff_prev ≤ 0 或 Price ≤ SMA50) → 不开仓（一日游反弹或中期趋势向下的反弹）
  DI diff < 0  → 不开仓（不做空，3x 做空在牛市长周期中持续损耗）
  DI diff = 0  → 不开仓
  死亡交叉期间（SMA50 < SMA200）且 price > SMA200 且 SMA50_5日变化 < 2 → 不开仓（熊市假反弹陷阱）
```
为什么不做空？
  6 年回测 PUT×3 净亏损。3x 做空在长期牛市中持续损耗，仅在极端恐慌时短暂盈利，
  但恐慌期间做空也会被剧烈反弹止损，长期净效应为负。详见 `tests/hybrid.py` 顶部注释。

为什么死亡交叉 + SMA50 躺平要过滤？
  死亡交叉期间 price > SMA200 说明是反弹到均线上方而非趋势反转，
  此时若 SMA50 还在下跌或横盘（5日变化 < 2），反弹大概率失败。
  2022 年 2 笔 L1 入场因此过滤，节省 $1,133；2025 年复苏期 SMA50 快速上升（+4.4）不受影响。

### Level 2 — 近轨 + VIX 高 → 反转确认（覆盖趋势方向，只做CALL）
```
近轨 + score ≥ 50 + VIX% > 75
   贴 BB 下轨 → CALL
```
为什么 VIX>75 + 贴下轨 → CALL？
  VIX 百分位 >75 = 市场恐慌阶段，通常对应局部底部
  近 BB 下轨 + 恐慌抛售 = 高概率反弹（恐慌卖出后的均值回归）
  不做近顶做空（即使 VIX 高）：近轨 + VIX 高时做空 PUT 在牛市长周期中严重亏损

### Level 3 — 矛盾过滤（仅保留近顶冲突）
```
近 BB 上轨 + DI diff > 0 → 不开仓（趋势看多但价格在顶）
# 近 BB 下轨 + DI diff < 0 → 取消拦截，下放到 L4 处理（允许逆势抄底）
```

### Level 4 — 近轨（只做CALL，score≥35 + ADX<25 或 RSI<35）
```
近 BB 下轨 + score ≥ 35 + (ADX<25 或 RSI<35) + 非(ADX≥25 且 Price<SMA50) → CALL
近 BB 下轨 + (ADX≥25 且 RSI≥35)                                         → 不开仓
近 BB 下轨 + (ADX≥25 且 Price<SMA50)                                     → 不开仓（强趋势下跌中不接飞刀）
近 BB 但 score < 35                                                       → 不开仓
BB 中段                                                                    → 不开仓
```
为什么 ADX≥25+RSI≥35 不开仓？
  强趋势中（ADX≥25）价格可连续多日贴下轨，RSI 可在 30-40 区域长期徘徊
  RSI≥35 说明还没到真正超卖，此时抄底 = 接飞刀
为什么 ADX≥25+Price<SMA50 不开仓？
  强下跌趋势中（ADX≥25），价格在 SMA50 下方运行，说明空头趋势牢固
  此时即使贴 BB 下轨，也大概率继续下跌而非均值回归（2024 年 4 月验证）
  CALL 仅在以下条件成立时有效：
    · ADX<25（趋势弱，均值回归概率高）
    · RSI<35（真正超卖，短期反弹概率大）
  两者都不满足 → 不开仓

### 近轨判定
```
near_threshold = ATR14 × 0.60
near_top      = (BBU - price) < near_threshold
near_bottom   = (price - BBL) < near_threshold
```

---

## 4. 仓位（$5k SPXL 3x + 1张滚动CALL价差）

| 开仓条件 | ETF 仓位 | 期权 | 备注 |
|---------|---------|------|------|
| score ≥ 50 + 方向有效 | **$5,000**（1仓SPXL 3x） | **1张14DTE ATM CALL价差15点**（滚动） | 同时开立，同进同出 |

单笔最大投入 **$5,000**（3x ETF），本金 $10,000。
CALL价差为 **滚动持仓**（DTE≤2滚至下一期14DTE），入场 ATM（Δ≈0.50）买K1、卖K1+15，消亡前一直持有。
CALL价差入仓即加入Moomoo watchlist。

### 强度标签（仅用于显示）
| 标签 | 条件 |
|------|------|
| **strong** | score ≥ 72 + \|DI diff\| > 0 |
| **normal** | score ≥ 65（非 strong） |
| **weak** | score < 65（有方向） |



---

## 5. 树结构（Christmas Tree）

> ⚠️ 树已从盘中报告中移除（2026-08-02），不再作为策略建议。以下规则仅保留给 **UI 圣诞树专区 / 自选 / 历史** 使用。

### 统一规则（7 DTE）
| 场景 | 中腿偏移 | S 腿 | L 腿 |
|------|----------|------|------|
| 趋势 + CALL | EMA20 + 5 | M - 10 | M + 5 |
| 趋势 + PUT | EMA20 - 5 | M + 10 | M - 5 |
| 震荡 | EMA20 ± 0 | M + 10 (PUT) / M - 10 (CALL) | M - 5 (PUT) / M + 5 (CALL) |

### 价格公式
```
PUT 树: 组合值 = S_mid + 2 × L_mid - 3 × M_mid
CALL 树: 组合值 = S_mid + 2 × L_mid - 3 × M_mid  （算法相同）
```

- 行权价均按 5 取整（`_s5()`）

---

## 6. 滚动CALL价差（趋势专用）

趋势方向时自动开立 **1张14DTE ATM CALL价差15点**（买K1=Δ≈0.50，卖K1+15），与$5k SPXL 3x同时入场。

### 6.1 入场过滤（高位不追）
新开仓要求 **BB% ≤ 80%**（报告 dlow ≤ 80）。Bollinger 上轨附近（高位）不追高，等待回落后的下一个信号再入场。
**已有持仓不受影响**：过滤仅约束新开仓（滚仓照常、出场规则不变）。
回测：跳过 BB%>80 后 27tr→21tr，WR 70%→71%，max亏 -$1,206→-$1,022（纯ETF腿 WR 67%→76%、亏损合计近减半）。

### 6.2 滚仓规则
- 到期日 **DTE ≤ 2** 时滚至下一期（14DTE ATM 价差15点）
- 关闭旧价差（按当时两腿mid价结算净借，累计PnL）并开立新价差
- 新仓自动加入Moomoo watchlist

### 6.3 出场条件
CALL价差 **共享ETF的XSP出场触发条件**（XSP 3%回落跟踪 / XSP 5%固定止损 / XSP 2%入场硬止损），无独立期权止损。
即XSP触发退出时，ETF和CALL价差同时平仓。

---

## 7. 持有计划

| 标的 | 持有期限 |
|------|----------|
| ETF（SPXL 3x） | **t+30 收盘平仓**（XSP价格追踪 3% 先触发则提前出） |
| 滚动CALL价差 | **共享ETF出场**（无独立期限；滚动至趋势结束或XSP触发退出） |

趋势结束时（direction变为None或score < 50）CALL价差随ETF一起平仓。
滚动CALL价差通过滚仓机制解决到期问题，不持有至expiry。

为什么 30 天？
  3x 杠杆需要时间复合趋势。10 年实测对比：
  · T+3=$5,998、T+5=$6,146、T+7=$5,705、T+10=$5,274、T+14=$5,706 → 全部跑输 T+30=$8,186
  · 短期持有刚建仓就被 1-3% 波动止损，3x 杠杆的优势无法发挥
  · 30 天 ≈ 6 交易周，覆盖大多数中短期趋势的完整持续期
  · T+60 无明显额外提升（仍约 $8k），说明 30 天后趋势已充分兑现

### 平仓触发条件（自动提示）
1. DTE ≤ 3
2. 盈利 ≥ 50% 或 亏损 ≥ 50%
3. BB 中段 + score < 65（建议减仓）
4. 趋势结束（上期 score ≥ 65 → 本期 < 65）
5. 方向冲突（持仓方向与当前信号不同）
6. 周末持仓风险（周五）
7. 浮亏达最大损失 80%
8. 方向切换（上期方向 ≠ 本期方向）

---

## 8. 风控规则（趋势: 全部XSP基准）

> **基准说明**：趋势策略的固定止损、跟踪回落、入场硬止损均以 **XSP（大盘）** 价格为基准。
> 3x ETF（SPXL）的对应百分比 ≈ XSP百分比 × 3。例如 XSP 5%固定止损 ≈ ETF 15%。
> 报告会自动显示 XSP 止损价 + ETF 等价换算。

### 8.1 方案概览

| 策略 | 价格追踪(XSP) | 固定止损(XSP) | 持仓上限 |
|------|---------|---------|---------|
| **趋势** | peak × 0.97（XSP回落3%） | entry × 0.95（XSP跌5%） | t+30 |

有效止损价 = **min(固定止损价, 跟踪止损价)**。

### 8.2 固定止损（Moomoo止蚀盘）
| 信号类型 | stop%(XSP) | 触发价(XSP) | 覆盖风险 |
|---------|-------|--------|---------|
| **趋势** | **5.0%** | entry × 0.95（XSP） | 跳空暴跌 |
| **趋势nearBB** | **1.0%** | entry × 0.99（XSP） | 反转失败 |

Moomoo止蚀盘按报告显示的 `≈ETF $xx` 值设定（自动换算）。

开仓一次性设定，无需后续修改。期权价差/树不做止损。

### 8.3 入场硬止损（从未涨→跌2% XSP退）
```
条件: peak_price < entry_price × 1.005（从未涨超0.5%）
触发: 收盘价 ≤ entry_price × 0.98（XSP跌2%）
       ≈ ETF跌6%（3x杠杆）
```

为什么 2%？7/9 亏损笔从未涨超 1%，入场即跌。等 3% trail 不如早切。

### 8.4 价格追踪（趋势3%）
```
趋势LONG: 追踪价 = peak_price × 0.97
```
- `peak_price` = 持有期内最高 XSP 收盘价（LONG）
- 仅当 `peak_price > entry_price` 时追踪价才有效（未盈利时只用固定止损）
- 3倍ETF: XSP 1% ≈ ETF 3%（3x杠杆）

为什么趋势 3%？
  趋势（30 天持有）：正常日波动 ±0.5-1%。3% 回落才触发说明趋势已真正衰竭，不是日常噪音

### 8.5 单笔最大亏损
单仓 $5,000，3x $15,000 名义：

| 触发方式 | 最大亏损（单仓） | 说明 |
|---------|---------|------|
| 趋势追踪3% | $5k×9% = **$450** | 日常上限（从peak回落3%XSP≈9% ETF） |
| 入场硬止损2% | $5k×6% = **$300** | 未涨即跌，提前切（≈XSP跌2%） |
| 趋势固定5% | $5k×15% = **$750** | 跳空兜底 |

### 8.6 CALL价差风控说明
- CALL价差 **无独立止损/止盈**，共享ETF的XSP出场触发条件：
  - XSP 3%回落跟踪触发 → 手工平ETF+CALL价差
  - XSP 5%固定止损（Moomoo止蚀盘，按报告≈ETF值设定）→ 自动平ETF，手工平CALL价差
  - XSP 2%入场硬止损 → 手工平ETF+CALL价差
- CALL价差最大亏损 = 入场净借 + 累计滚仓亏损（滚仓次数×单次亏损），单次滚仓亏损下限 = 净借×100

### 8.7 执行流程
1. 开仓：买入 3x ETF，入1张14DTE ATM CALL价差15点（买Δ0.50 ATM，卖K1+15），设 Moomoo 止蚀盘（按报告`≈ETF $xx`值设定）
2. 不设止盈限价单
3. 每天看报告：有效XSP止损价 = min(固定, 追踪)；追踪价触发→手工平ETF+CALL价差；固定价触发→Moomoo自动平ETF，手工平CALL价差
4. DTE≤2时滚至下一期14DTE
5. 第 30 天手工平仓ETF+CALL价差

---

## 10. 报告时间

| 报告 | 悉尼时间 | 东部时间 | 对应 DTE 调整 |
|------|----------|----------|---------------|
| **早报** | 21:25-21:35 | ~07:25-07:35 ET | dte_adj = 0 |
| **晚报** | ≈06:30-08:30（=美东收盘+30分钟，随冬夏令时换算） | 16:25-16:35 ET（收盘后30分） | dte_adj = 1 |

- 仅工作日（周一至周五）运行
- 重复报告 120 秒去重
- 新客户端连接时 force 生成（跳过 Telegram）

---

## 11. 持仓追踪

- `_active_position_date` 在方向不变时持续累计
- 方向切换时重置为当日
- 状态持久化至 `position_tracker.json`

---

## 12. 均值回归（MR 裸买CALL）

仅在**趋势无信号**时触发（`direction = None`, BB 中段）。

### 12.1 入场条件（两项全满足）
1. `rsi_14 < 30` — 超卖
2. `VIX > 20` — 恐慌环境

### 12.2 仓位
- 买 1 张 7DTE CALL 期权 + **开 $2k SPXL 3x ETF**，独立追踪
- **ATM（行权价=$XSP入場÷5×5）**: 成本≈$7.7/股（$770/口）
- **OTM Δ0.35**: 成本≈$4.6/股（$460/口）
- 实际入场以Moomoo实时报价为准，以上为BS理论价
- 重放均单（仅 2 笔，样本极小）: CALL ≈ +$110/口 + ETF ≈ +$75/单

### 12.3 出场条件（优先级从高到低）
1. **-2% XSP止损**: 收盘价 ≤ 入场价 × 0.98 → 提前退出（截住深跌）
2. **+0.3%首阳**: 收盘价 > 入场价 × 1.003 → 盈利平仓（0.3%缓冲滤掉theta吃掉的微利）
3. **T+3**: 持有 ≥ 3 个交易日 → 强制平仓
4. **最大亏损**: 全部权利金（$770/口 ATM前提）

### 12.4 风险限制
- 不同时持有趋势仓或崩盘仓（趋势/崩盘已满仓时不做 MR；2026-07-31 起双向互斥）
- **结构性低频**：MR 信号日=恐慌日（RSI<30+VIX>20），与崩盘开仓日（XSP 单日跌>0.5%）高度重叠，且 direction 闸要求方向转空——恐慌日几乎总被崩盘层先承接，故 MR 年频远低于旧口径「4-5 次」。
- 回测（app 重放，2021-06→2026-08, 5.4y）: **2tr, WR 1/2, 总PnL +$370**（2022-01-26 +$491 大赚 / 2022-09-23 −$121：首阳触发退出仍可能因波动率坍塌亏损）
- 最大亏损 = 全部权利金（$460-770/口）

---

## 13. 崩盘反弹（CALL价差15点 21DTE + $2k SPXL）

第三层策略，仅在趋势和MR均无信号时独立触发。

### 13.1 入场条件
- `XSP当日跌 > 0.5%` — 单日下跌（基于收盘价对比前收盘）
- 无VIX要求

### 13.2 仓位
- **CALL价差15点**: 买1张21DTE ATM CALL + 卖1张(ATM+15) CALL
  - 均成本≈$5.9/股（$587/口），最大亏损=$净权利金≤$1,500
  - 最大盈利 = $1,500 - 成本（约$913/口）
- **$2k SPXL 3x ETF**: 独立追踪
- 入场以Moomoo实时报价为准，以上为BS理论价

### 13.3 出场条件（优先级从高到低）
期权**单口**，首次阳线**全平**；ETF **$2k 分两半**：首阳平一半（$1k），剩余一半在**二次阳线 / 止损 / T+4**时平。
1. **-2.5% XSP止损**: XSP价 ≤ 入场价 × 0.975 → 平仓（未缩放=期权+全ETF；已缩放=剩余ETF一半）
2. **首阳(0%)**: XSP价 > 入场价 且持有 ≤ 4天 → 期权全平 + ETF退一半（$1k），剩 $1k 续持
3. **二次首阳**: 已缩放后 XSP价 再 > 入场价 → 剩余ETF一半平仓
4. **T+4**: 持有 ≥ 4个交易日 → 强制平仓（未缩放=全平；已缩放=剩余ETF一半）
5. **最大亏损**: 净权利金（约$587/口）+ ETF腿跌幅

### 13.4 风险限制
- 不同时持有趋势仓或MR仓
- 年交易约 32 次（5.4y 重放 174 笔）
- 回测（app 重放，2021-06→2026-08, 5.4y）:
  **174tr, WR 136/174 (78%), 总PnL +$8,480**（ETF 腿 + 期权腿合计；均持≈3.4d）
- **最大单笔亏损**: -$767（期权腿 + ETF腿合计）
- 最大亏损 = 净权利金 + ETF腿跌幅（约$1,050）
