# XSP 交易规则文档

## 1. 核心参数

### 分数权重
| 指标 | 权重 | 分档（100 / 75 / 50 / 25 / 0） |
|------|------|----------------------------------|
| ADX  | 30%  | ≥30 / ≥25 / ≥20 / ≥15 / <15 |
| ER   | 20%  | ≥0.70 / ≥0.55 / ≥0.35 / ≥0.20 / <0.20 |
| BBW  | 15%  | ≥45 / ≥30 / ≥18 / ≥10 / <10 |
| Dev  | 15%  | ≥3.0 / ≥1.5 / ≥0.8 / ≥0.3 / <0.3 |
| VR   | 10%  | ≥2.0 / ≥1.3 / ≥0.8 / ≥0.5 / <0.5 |

- 公式：`score = 5 + Σ(分数(sub_val, T[k]) × W[k])`，四舍五入取整

### 关键阈值
| 参数 | 值 |
|------|----|
| is_trend | score ≥ 60 |
| 显示标签 Trending | score ≥ 65 |
| 近轨 ATR 乘数 | ATR14 × 0.60 |
| 近轨 BW 回退 | BW × 0.10（ATR14 不可用时） |
| 近轨方向分数下限 | score ≥ 60 |
| VIX 百分位阈值 | > 75 |
| 期权 DTE | 15 天 |
| 树 DTE | 7 天 |

---

## 2. 方向逻辑（4 层判断，从上到下）

### Level 1 — 趋势（非近轨 + is_trend）
```
非近轨 + score ≥ 50 → 趋势方向
  DI diff > 0 + di_diff_prev > 0 + Price > SMA50 → CALL（需 2 天连续 DI+ 确认 + 价格在 50 日均线上方）
  DI diff > 0 + (di_diff_prev ≤ 0 或 Price ≤ SMA50) → 不开仓（一日游反弹或中期趋势向下的反弹）
  DI diff < 0  → PUT（仅在 VIX>80% 或 VIX>28 时）
  DI diff = 0  → 不开仓
```

### Level 2 — 近轨 + VIX 高 → 反转确认（覆盖趋势方向，只做CALL）
```
近轨 + score ≥ 50 + VIX% > 75
   贴 BB 下轨 → CALL
```

### Level 3 — 矛盾过滤（仅保留近顶冲突）
```
近 BB 上轨 + DI diff > 0 → 不开仓（趋势看多但价格在顶）
# 近 BB 下轨 + DI diff < 0 → 取消拦截，下放到 L4 处理（允许逆势抄底）
```

### Level 4 — 近轨（只做CALL，score≥35 + ADX<25 或 RSI<35）
```
近 BB 下轨 + score ≥ 35 + (ADX<25 或 RSI<35) → CALL
近 BB 下轨 + (ADX≥25 且 RSI≥35)             → 不开仓（强趋势不抄底）
近 BB 但 score < 35                           → 不开仓
BB 中段                                        → 不开仓
```

### 近轨判定
```
near_threshold = ATR14 × 0.60
near_top      = (BBU - price) < near_threshold
near_bottom   = (price - BBL) < near_threshold
```

---

## 3. VIX 做空条件

| 方向 | 条件 | 说明 |
|------|------|------|
| PUT | VIX百分位 > 80 **或** VIX价格 > 28 | 仅Level 1趋势做空，Level 2/4近轨不做空 |

SKEW 来源：`^SKEW` via yfinance。

---

## 4. 仓位（双仓位，总 $5k，Cap$5k ×1.5）

| 开仓条件 | ETF 仓位 | 裸买 | 备注 |
|---------|---------|------|------|
| score ≥ 50 + 方向有效 | **$2,500/仓**（最多2仓） | score≥65:1张/ else:0 | 有空槽即可入仓，同向/反向均允许 |

单笔最大投入 **$2,500**（3x ETF），总持仓 **$5,000**，本金 $10,000。

### 强度标签（仅用于显示）
| 标签 | 条件 |
|------|------|
| **strong** | score ≥ 72 + \|DI diff\| > 0 + skew_confirm |
| **normal** | score ≥ 65（非 strong） |
| **weak** | score < 65（有方向） |

### skew_confirm 判定
```python
skew_confirm = (direction == 'CALL' and SKEW < 145) or (direction == 'PUT' and SKEW > 145)
```



---

## 5. 树结构（Christmas Tree）

### 统一规则（7 DTE）
| 场景 | 中腿偏移 | S 腿 | L 腿 |
|------|----------|------|------|
| 趋势 + CALL | EMA20 + 5 | M - 10 | M + 5 |
| 趋势 + PUT | EMA20 - 5 | M + 10 | M - 5 |
| 震荡 | EMA20 ± 0 | M + 10 (PUT) / M - 10 (CALL) | M - 5 (PUT) / M + 5 (CALL) |

### 价格公式
```
PUT 树: 组合值 = S_mid + 2 × L_mid - 3 × M_mid
CALL 树: 组合值 = S_mid + 2 × L_mid - 3 × M_mid  （算法相同）
```

- 行权价均按 5 取整（`_s5()`）

---

## 6. 裸买（单腿）

- 仅当 `is_trend = True`（score ≥ 60）
- 7 DTE，Δ ≈ 0.35（CALL）或 Δ ≈ -0.35（PUT）
- 仅出现在 strong（最多 2 张）或 normal（1 张）等级

---

## 7. 持有计划

| 标的 | 持有期限 |
|------|----------|
| 全部（树/裸/ETF） | **t+30 收盘平仓**（价格追踪 3% 先触发则提前出） |

### 平仓触发条件（自动提示）
1. DTE ≤ 3
2. 盈利 ≥ 50% 或 亏损 ≥ 50%
3. BB 中段 + score < 65（建议减仓）
4. 趋势结束（上期 score ≥ 65 → 本期 < 65）
5. 方向冲突（持仓方向与当前信号不同）
6. 周末持仓风险（周五）
7. 浮亏达最大损失 80%
8. 方向切换（上期方向 ≠ 本期方向）

---

## 8. 风控规则（价格追踪3% + 固定止损兜底）

### 8.1 方案概览

| 层 | 触发条件 | 操作 | 说明 |
|----|---------|------|------|
| **价格追踪** | peak_price × 0.97（从最高价回落3%） | 报告提示→手工平 | 主要风控，正常情况优先触发 |
| **固定止损（兜底）** | trend: entry×0.95 / nearBB: entry×0.99 | Moomoo自动成交 | 防开盘跳空，日常不会触及 |
| **t+30** | 第30天 | 手工平 | 到期平仓 |

有效止损价 = **min(固定止损价, 跟踪止损价)**。价格上涨时跟踪价随peak上移并低于固定价（正常跟踪先触发），下降时固定价兜底。

### 8.2 固定止损（Moomoo止蚀盘）
| 信号类型 | stop% | 触发价 | 覆盖风险 |
|---------|-------|--------|---------|
| **趋势** | **5.0%** | entry × 0.95 | 跳空暴跌 |
| **nearBB** | **1.0%** | entry × 0.99 | 反转失败 |

开仓一次性设定，无需后续修改。期权裸买/树不做止损。

### 8.3 价格追踪（从最高价回落3%）
```
追踪价 = peak_price × 0.97
```
- `peak_price` = 持有期内最高 XSP 收盘价（每日更新，持久化至 position_tracker.json）
- 仅当 `peak_price > entry_price` 时追踪价才有效（未盈利时只用固定止损）
- 不设自动单，每天看报告判断：现价 ≤ 追踪价？手工平仓
- 上涨时追踪价随peak上移并逐渐低于固定止损价（正常追踪先触发）
- 3倍ETF: XSP 3% ≈ ETF 9%（3x杠杆）

### 8.4 单笔最大亏损
最大单仓 $2,500（双仓位总 $5,000，3x $7,500 名义）：

| 触发方式 | 最大亏损（单仓） | 说明 |
|---------|---------|------|
| 价格追踪3% | $2.5k×9% = **$225** | 日常上限（从peak回落3%XSP≈9% ETF） |
| 固定趋势5% | $2.5k×15% = **$375** | 跳空兜底 |
| 固定nearBB 1% | $2.5k×3% = **$75** | 跳空兜底 |

双仓同时触发时总亏损约 **$450-$750**，本金 $10,000。

### 8.5 执行流程
1. 开仓：买入 3x ETF，设 Moomoo 止蚀盘（trend: entry×0.95, nearBB: entry×0.99）
2. 不设止盈限价单
3. 每天看报告：有效止损价 = min(固定, 追踪)；追踪价触发→手工平；固定价触发→Moomoo自动平
4. 第 30 天手工平仓

---

## 9. 报告时间

| 报告 | 悉尼时间 | 东部时间 | 对应 DTE 调整 |
|------|----------|----------|---------------|
| **早报** | 21:25-21:35 | ~07:25-07:35 ET | dte_adj = 0 |
| **晚报** | 09:25-09:35 | ~19:25-19:35 ET | dte_adj = 1 |

- 仅工作日（周一至周五）运行
- 重复报告 120 秒去重
- 新客户端连接时 force 生成（跳过 Telegram）

---

## 10. 持仓追踪

- `_active_position_date` 在方向不变时持续累计
- 方向切换时重置为当日
- 状态持久化至 `position_tracker.json`
